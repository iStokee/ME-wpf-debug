<UserControl
    x:Class="MESharp.Views.ObjectsView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:sys="clr-namespace:System;assembly=mscorlib"
    Background="{DynamicResource App.BackgroundBrush}"
    Foreground="{DynamicResource App.ForegroundBrush}">

    <ScrollViewer>
        <StackPanel Margin="12">
            <GroupBox Header="Objects">
                <StackPanel>
                    <StackPanel Orientation="Horizontal">
                        <TextBox Width="240" Text="{Binding FilterText, UpdateSourceTrigger=PropertyChanged}" />
                        <TextBlock Margin="8,0,4,0" VerticalAlignment="Center" Text="Action:" />
                        <ComboBox Width="60" SelectedIndex="{Binding ActionIndex}">
                            <ComboBox.ItemsSource>
                                <x:Array Type="{x:Type sys:Int32}">
                                    <sys:Int32>0</sys:Int32>
                                    <sys:Int32>1</sys:Int32>
                                    <sys:Int32>2</sys:Int32>
                                    <sys:Int32>3</sys:Int32>
                                    <sys:Int32>4</sys:Int32>
                                    <sys:Int32>5</sys:Int32>
                                    <sys:Int32>6</sys:Int32>
                                    <sys:Int32>7</sys:Int32>
                                    <sys:Int32>8</sys:Int32>
                                    <sys:Int32>9</sys:Int32>
                                    <sys:Int32>10</sys:Int32>
                                </x:Array>
                            </ComboBox.ItemsSource>
                        </ComboBox>
                        <Button Width="120" Margin="8,0,0,0" Command="{Binding DoActionCommand}" Content="DoAction" Style="{StaticResource AccentButton}" />
                        <Button Width="80" Margin="8,0,0,0" Command="{Binding RefreshCommand}" Content="Refresh" Style="{StaticResource AccentButton}" />
                    </StackPanel>

                    <ListView Margin="0,8,0,0" ItemsSource="{Binding ObjectsView}" Background="{DynamicResource App.CardBrush}" ScrollViewer.HorizontalScrollBarVisibility="Auto">
                        <ListView.View>
                            <GridView>
                                <GridViewColumn Width="80" DisplayMemberBinding="{Binding Id}" Header="ID" />
                                <GridViewColumn Width="240" DisplayMemberBinding="{Binding Name}" Header="Name" />
                                <GridViewColumn Width="80" DisplayMemberBinding="{Binding Type}" Header="Type" />
                                <GridViewColumn Width="80" DisplayMemberBinding="{Binding Distance}" Header="Dist" />
                                <GridViewColumn Width="160" Header="Tile">
                                    <GridViewColumn.CellTemplate>
                                        <DataTemplate>
                                            <TextBlock>
                                                <Run Text="{Binding Tile.x}" />
                                                <Run Text="," />
                                                <Run Text="{Binding Tile.y}" />
                                                <Run Text="," />
                                                <Run Text="{Binding Tile.z}" />
                                            </TextBlock>
                                        </DataTemplate>
                                    </GridViewColumn.CellTemplate>
                                </GridViewColumn>
                            </GridView>
                        </ListView.View>
                    </ListView>
                </StackPanel>
            </GroupBox>
        </StackPanel>
    </ScrollViewer>
</UserControl>
