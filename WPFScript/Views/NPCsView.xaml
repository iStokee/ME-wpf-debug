<UserControl
    x:Class="MESharp.Views.NPCsView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:vm="clr-namespace:MESharp.ViewModels"
    Background="{DynamicResource App.BackgroundBrush}"
    Foreground="{DynamicResource App.ForegroundBrush}">
    <UserControl.DataContext>
        <vm:NpcViewModel />
    </UserControl.DataContext>
    <ScrollViewer>
    <GroupBox Header="NPCs" Margin="8">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <!--  Filters + Action  -->
        <StackPanel
            Grid.Row="0"
            Margin="0,0,0,8"
            VerticalAlignment="Center"
            Orientation="Horizontal">
            <TextBlock
                Margin="5,5,5,7"
                VerticalAlignment="Center"
                Text="Filter (name or ID):" />
            <TextBox
                Width="150"
                Margin="5"
                Text="{Binding FilterText, UpdateSourceTrigger=PropertyChanged}" />
            <Ellipse Width="10" Height="10" Margin="8,0,6,0">
                <Ellipse.Style>
                    <Style TargetType="Ellipse">
                        <Setter Property="Fill" Value="{DynamicResource App.BorderBrush}" />
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding ShowOnlyAlive}" Value="True">
                                <Setter Property="Fill" Value="{DynamicResource PrimaryBrush}" />
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </Ellipse.Style>
            </Ellipse>
            <CheckBox Margin="5" VerticalAlignment="Center" Content="Show Only Alive" IsChecked="{Binding ShowOnlyAlive}" />

            <TextBlock
                Margin="5,5,5,7"
                VerticalAlignment="Center"
                Text="Action #:" />
            <ComboBox
                Width="50"
                ItemsSource="{Binding ActionIndices}"
                SelectedItem="{Binding ActionIndex}" />
            <Button
                Margin="5"
                Command="{Binding DoActionCommand}"
                Content="Do Action"
                Style="{StaticResource AccentButton}" />
        </StackPanel>

        <!--  Unified NPC Grid  -->
        <DataGrid
            Grid.Row="1"
            AutoGenerateColumns="False"
            CanUserSortColumns="True"
            CanUserResizeColumns="True"
            ColumnWidth="SizeToCells"
            IsReadOnly="True"
            Background="{DynamicResource App.CardBrush}"
            ItemsSource="{Binding NpcsView}"
            SelectedItem="{Binding SelectedNpc, Mode=TwoWay}">
            <DataGrid.Columns>
                <DataGridTextColumn Binding="{Binding Name}" Header="Name" SortMemberPath="Name" Width="*" MinWidth="200" />
                <DataGridTextColumn Binding="{Binding Id}" Header="ID" SortMemberPath="Id" Width="80" />
                <DataGridTextColumn Binding="{Binding Health}" Header="Health" SortMemberPath="Health" Width="90" />
                <DataGridTextColumn Binding="{Binding X}" Header="X" SortMemberPath="X" Width="70" />
                <DataGridTextColumn Binding="{Binding Y}" Header="Y" SortMemberPath="Y" Width="70" />
                <DataGridTextColumn Binding="{Binding Distance}" Header="Distance" SortMemberPath="Distance" Width="90" />
                <DataGridTextColumn Binding="{Binding Animation}" Header="Animation" SortMemberPath="Animation" Width="100" />
                <DataGridTextColumn Binding="{Binding UniqueId}" Header="UniqueId" SortMemberPath="UniqueId" Width="120" />
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
    </GroupBox>
    </ScrollViewer>
</UserControl>
