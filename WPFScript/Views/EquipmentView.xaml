<UserControl
    x:Class="MESharp.Views.EquipmentView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    Background="{DynamicResource App.BackgroundBrush}"
    Foreground="{DynamicResource App.ForegroundBrush}">

    <ScrollViewer>
        <StackPanel Margin="12">
            <GroupBox Header="Equipment">
                <StackPanel>
                    <!-- State + Load -->
                    <StackPanel Orientation="Horizontal">
                        <Button Width="100" Command="{Binding RefreshStateCommand}" Content="Refresh State" Style="{StaticResource AccentButton}" />
                        <Button Width="100" Margin="8,0,0,0" Command="{Binding LoadItemsCommand}" Content="Load Items" Style="{StaticResource AccentButton}" />
                        <Ellipse Width="10" Height="10" Margin="12,0,6,0">
                            <Ellipse.Style>
                                <Style TargetType="Ellipse">
                                    <Setter Property="Fill" Value="{DynamicResource App.BorderBrush}" />
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding IsOpen}" Value="True">
                                            <Setter Property="Fill" Value="{DynamicResource PrimaryBrush}" />
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </Ellipse.Style>
                        </Ellipse>
                        <TextBlock FontWeight="SemiBold" Text="Open:" />
                        <TextBlock Text="{Binding IsOpen}" />
                        <Ellipse Width="10" Height="10" Margin="8,0,6,0">
                            <Ellipse.Style>
                                <Style TargetType="Ellipse">
                                    <Setter Property="Fill" Value="{DynamicResource App.BorderBrush}" />
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding IsEmpty}" Value="True">
                                            <Setter Property="Fill" Value="{DynamicResource PrimaryBrush}" />
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </Ellipse.Style>
                        </Ellipse>
                        <TextBlock FontWeight="SemiBold" Text="Empty:" />
                        <TextBlock Text="{Binding IsEmpty}" />
                        <Ellipse Width="10" Height="10" Margin="8,0,6,0">
                            <Ellipse.Style>
                                <Style TargetType="Ellipse">
                                    <Setter Property="Fill" Value="{DynamicResource App.BorderBrush}" />
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding IsFull}" Value="True">
                                            <Setter Property="Fill" Value="{DynamicResource PrimaryBrush}" />
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </Ellipse.Style>
                        </Ellipse>
                        <TextBlock FontWeight="SemiBold" Text="Full:" />
                        <TextBlock Text="{Binding IsFull}" />
                    </StackPanel>

                    <!-- Filter & Indicators + Action -->
                    <StackPanel Orientation="Horizontal" Margin="0,8,0,0">
                        <TextBlock Margin="0,0,6,0" VerticalAlignment="Center" Text="Filter (IDs or Name):" />
                        <TextBox Width="260" Text="{Binding QuickFilterText, UpdateSourceTrigger=PropertyChanged}" />
                        <TextBlock Margin="12,0,6,0" VerticalAlignment="Center" Text="Count:" />
                        <TextBlock VerticalAlignment="Center" Text="{Binding QuickCount}" />
                        <Ellipse Width="10" Height="10" Margin="12,0,6,0">
                            <Ellipse.Style>
                                <Style TargetType="Ellipse">
                                    <Setter Property="Fill" Value="{DynamicResource App.BorderBrush}" />
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding ContainsAnyFlag}" Value="True">
                                            <Setter Property="Fill" Value="{DynamicResource PrimaryBrush}" />
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </Ellipse.Style>
                        </Ellipse>
                        <TextBlock VerticalAlignment="Center" Text="Any:" />
                        <TextBlock VerticalAlignment="Center" Text="{Binding ContainsAnyResult}" />
                        <Ellipse Width="10" Height="10" Margin="6,0,6,0">
                            <Ellipse.Style>
                                <Style TargetType="Ellipse">
                                    <Setter Property="Fill" Value="{DynamicResource App.BorderBrush}" />
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding ContainsAllFlag}" Value="True">
                                            <Setter Property="Fill" Value="{DynamicResource PrimaryBrush}" />
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </Ellipse.Style>
                        </Ellipse>
                        <TextBlock VerticalAlignment="Center" Text="All:" />
                        <TextBlock VerticalAlignment="Center" Text="{Binding ContainsAllResult}" />
                        <Ellipse Width="10" Height="10" Margin="6,0,6,0">
                            <Ellipse.Style>
                                <Style TargetType="Ellipse">
                                    <Setter Property="Fill" Value="{DynamicResource App.BorderBrush}" />
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding ContainsOnlyFlag}" Value="True">
                                            <Setter Property="Fill" Value="{DynamicResource PrimaryBrush}" />
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </Ellipse.Style>
                        </Ellipse>
                        <TextBlock VerticalAlignment="Center" Text="Only:" />
                        <TextBlock VerticalAlignment="Center" Text="{Binding ContainsOnlyResult}" />
                        <Ellipse Width="10" Height="10" Margin="12,0,6,0">
                            <Ellipse.Style>
                                <Style TargetType="Ellipse">
                                    <Setter Property="Fill" Value="{DynamicResource App.BorderBrush}" />
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding QuickCount}" Value="0">
                                            <Setter Property="Fill" Value="{DynamicResource App.BorderBrush}" />
                                        </DataTrigger>
                                        <DataTrigger Binding="{Binding QuickCount}" Value="1">
                                            <Setter Property="Fill" Value="{DynamicResource PrimaryBrush}" />
                                        </DataTrigger>
                                        <DataTrigger Binding="{Binding QuickCount}" Value="2">
                                            <Setter Property="Fill" Value="{DynamicResource PrimaryBrush}" />
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </Ellipse.Style>
                        </Ellipse>
                        <TextBlock VerticalAlignment="Center" Text="Count:" />
                        <TextBlock VerticalAlignment="Center" Text="{Binding QuickCount}" />
                        <Button Width="120" Margin="12,0,0,0" Command="{Binding AutoUnequipCommand}" Content="Unequip" ToolTip="Uses ID if numeric, else Name" Style="{StaticResource AccentButton}" />
                    </StackPanel>

                    <!-- Filtered Items -->
                    <ListView Height="320" Margin="0,8,0,0" Background="{DynamicResource App.CardBrush}" ItemsSource="{Binding QuickItems}">
                        <ListView.View>
                            <GridView>
                                <GridViewColumn Width="70" Header="Slot" DisplayMemberBinding="{Binding Slot}" />
                                <GridViewColumn Width="80" Header="ID" DisplayMemberBinding="{Binding Id}" />
                                <GridViewColumn Width="240" Header="Name" DisplayMemberBinding="{Binding Name}" />
                                <GridViewColumn Width="100" Header="Amount" DisplayMemberBinding="{Binding Amount}" />
                                <GridViewColumn Width="80" Header="XP" DisplayMemberBinding="{Binding Xp}" />
                            </GridView>
                        </ListView.View>
                    </ListView>
                </StackPanel>
            </GroupBox>
        </StackPanel>
    </ScrollViewer>
</UserControl>
