<UserControl
    x:Class="MESharp.Views.BankView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    Background="{DynamicResource App.BackgroundBrush}"
    Foreground="{DynamicResource App.ForegroundBrush}">

    <ScrollViewer>
        <StackPanel Margin="12">
            <GroupBox Header="Bank">
                <StackPanel>
                    <!-- Filter + State -->
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Margin="0,0,6,0" VerticalAlignment="Center" Text="Filter (name or ID):" />
                        <TextBox Width="240" Text="{Binding QuickFilterText, UpdateSourceTrigger=PropertyChanged}" />
                        <Ellipse Width="10" Height="10" Margin="12,0,6,0">
                            <Ellipse.Style>
                                <Style TargetType="Ellipse">
                                    <Setter Property="Fill" Value="{DynamicResource App.BorderBrush}" />
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding HasStack}" Value="True">
                                            <Setter Property="Fill" Value="{DynamicResource PrimaryBrush}" />
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </Ellipse.Style>
                        </Ellipse>
                        <TextBlock VerticalAlignment="Center" Text="Stack:" />
                        <TextBlock VerticalAlignment="Center" Text="{Binding QuickStack}" />
                        <Button Width="120" Margin="12,0,0,0" Command="{Binding RefreshStateCommand}" Content="Refresh State" Style="{StaticResource AccentButton}" />
                        <Ellipse Width="10" Height="10" Margin="12,0,6,0">
                            <Ellipse.Style>
                                <Style TargetType="Ellipse">
                                    <Setter Property="Fill" Value="{DynamicResource App.BorderBrush}" />
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding IsOpen}" Value="True">
                                            <Setter Property="Fill" Value="{DynamicResource PrimaryBrush}" />
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </Ellipse.Style>
                        </Ellipse>
                        <TextBlock FontWeight="SemiBold" Text="Open:" />
                        <TextBlock Text="{Binding IsOpen}" />
                        <Button Width="100" Margin="12,0,0,0" Command="{Binding CloseCommand}" Content="Close" />
                    </StackPanel>

                    <!-- Deposit -->
                    <StackPanel Margin="0,8,0,0">
                        <TextBlock FontWeight="SemiBold" Text="Deposit" />
                        <StackPanel Orientation="Horizontal" Margin="0,4,0,0">
                            <Button Width="140" Content="Deposit All" Command="{Binding DepositAllCommand}" Style="{StaticResource AccentButton}" />
                            <TextBox Width="260" Margin="12,0,0,0" Text="{Binding IdsInput}" />
                            <Button Width="200" Margin="8,0,0,0" Content="Deposit All Except IDs" Command="{Binding DepositAllExceptIdsCommand}" Style="{StaticResource AccentButton}" />
                        </StackPanel>
                        <StackPanel Orientation="Horizontal" Margin="0,4,0,0">
                            <TextBox Width="260" Text="{Binding NamesInput}" />
                            <Button Width="220" Margin="8,0,0,0" Content="Deposit All Except Names" Command="{Binding DepositAllExceptNamesCommand}" Style="{StaticResource AccentButton}" />
                        </StackPanel>
                    </StackPanel>

                    <!-- Stacks -->
                    <StackPanel Margin="0,8,0,0">
                        <TextBlock FontWeight="SemiBold" Text="Stacks" />
                        <StackPanel Orientation="Horizontal" Margin="0,4,0,0">
                            <TextBox Width="120" Text="{Binding IdInput}" />
                            <Button Width="160" Margin="8,0,0,0" Content="Get Stack (ID)" Command="{Binding GetStackByIdCommand}" Style="{StaticResource AccentButton}" />
                            <TextBlock Margin="8,0,0,0" VerticalAlignment="Center" Text="{Binding StackResult}" />
                        </StackPanel>
                        <StackPanel Orientation="Horizontal" Margin="0,4,0,0">
                            <TextBox Width="240" Text="{Binding NameInput}" />
                            <Button Width="180" Margin="8,0,0,0" Content="Get Stack (Name)" Command="{Binding GetStackByNameCommand}" Style="{StaticResource AccentButton}" />
                        </StackPanel>
                    </StackPanel>

                    <!-- Actions -->
                    <StackPanel Margin="0,8,0,0">
                        <TextBlock FontWeight="SemiBold" Text="Actions" />
                        <StackPanel Orientation="Horizontal" Margin="0,4,0,0">
                            <TextBlock VerticalAlignment="Center" Text="Action:" />
                            <TextBox Width="60" Margin="4,0,8,0" Text="{Binding ActionIndex}" />
                            <TextBlock VerticalAlignment="Center" Text="Offset:" />
                            <TextBox Width="60" Margin="4,0,8,0" Text="{Binding Offset}" />
                            <TextBox Width="120" Text="{Binding IdInput}" />
                            <Button Width="160" Margin="8,0,0,0" Content="DoAction (ID)" Command="{Binding DoActionByIdCommand}" Style="{StaticResource AccentButton}" />
                            <TextBox Width="240" Margin="8,0,0,0" Text="{Binding NameInput}" />
                            <Button Width="180" Margin="8,0,0,0" Content="DoAction (Name)" Command="{Binding DoActionByNameCommand}" Style="{StaticResource AccentButton}" />
                            <TextBlock Margin="8,0,0,0" VerticalAlignment="Center" Text="{Binding ActionResult}" />
                        </StackPanel>
                    </StackPanel>
                </StackPanel>
            </GroupBox>
        </StackPanel>
    </ScrollViewer>
</UserControl>
